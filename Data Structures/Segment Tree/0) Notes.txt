

// Merge Sort Tree
ans.v.assign((ll)lf.v.size() + (ll)ri.v.size(), { });
merge(all(lf.v), all(ri.v), ans.v.begin());
ans.pre = ans.v;
for(ll i = 1; i < (ll)ans.pre.size(); i++)
    ans.pre[i] += ans.pre[i - 1];